{
  "name": "strip-ts-copy",
  "displayName": "Strip TS Copy",
  "description": "Strip types from TypeScript code while copying it.",
  "icon": "images/icon.png",
  "version": "0.11.0",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/anuraghazra/vscode-strip-ts-copy.git"
  },
  "publisher": "anuraghazra",
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "strip-ts-copy.copyAsJs",
        "title": "Copy TS as JS"
      }
    ],
    "keybindings": [
      {
        "command": "strip-ts-copy.copyAsJs",
        "key": "ctrl+alt+c",
        "mac": "shift+alt+cmd+c"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "strip-ts-copy.copyAsJs",
          "when": "editorHasSelection && editorLangId == typescript"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run esbuild-base -- --minify",
    "esbuild-base": "esbuild ./src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --external:prettier --format=cjs --platform=node",
    "compile": "yarn esbuild-base -- --sourcemap",
    "watch": "yarn esbuild-base -- --sourcemap --watch",
    "package": "yarn compile",
    "test-compile": "tsc -p . --outDir out",
    "pretest": "yarn run test-compile && yarn run compile && yarn run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^7.1.4",
    "@types/mocha": "^9.0.0",
    "@types/node": "14.x",
    "@types/vscode": "^1.69.0",
    "@typescript-eslint/eslint-plugin": "^4.31.1",
    "@typescript-eslint/parser": "^4.31.1",
    "@vscode/test-electron": "^1.6.2",
    "esbuild": "^0.13.2",
    "eslint": "^7.32.0",
    "glob": "^7.1.7",
    "mocha": "^9.1.1",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "@types/diff": "^5.0.3",
    "diff": "^5.1.0",
    "normalize-newline": "^4.1.0",
    "prettier": "^3.2.5"
  }
}